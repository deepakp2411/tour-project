import { banner } from "../data.js";

export const data = async (req,res) => {
    try {
        const result = ans.insertMany() 
        // res.status(201).json({msg:"craeted"})
    } catch (error) {
        console.log(error)
        
    }
}

